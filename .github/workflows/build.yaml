name: Deep Format USB Refresh Fix Gate

on:
  pull_request:
    branches: [ "main" ]

permissions:
  contents: write

env:
  PYTHONDONTWRITEBYTECODE: "1"

jobs:
  fix-and-verify:
    if: github.event.pull_request.head.ref == 'fix/deep-format-fatfs-work-area' && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/deep-format-fatfs-work-area
          fetch-depth: 0

      - name: Preserve an already-ejected medium during refresh
        run: |
          python3 - <<'PY'
          from pathlib import Path

          path = Path('src/msc_disk.c')
          raw = path.read_bytes()
          old = (
              b'void refresh_usbmsdrive(void) {\r\n'
              b'    // eject the usb drive\r\n'
          )
          new = (
              b'void refresh_usbmsdrive(void) {\r\n'
              b'    if (is_ejected()) {\r\n'
              b'        return;\r\n'
              b'    }\r\n'
              b'\r\n'
              b'    // eject the usb drive\r\n'
          )
          if raw.count(old) != 1:
              raise SystemExit(f'expected one refresh anchor, found {raw.count(old)}')
          path.write_bytes(raw.replace(old, new, 1))
          PY
          git -c core.whitespace=cr-at-eol diff --check -- src/msc_disk.c

      - name: Run host and source-contract tests
        run: |
          python3 -m unittest tests/test_deep_format_contract.py -v
          bash tests/host/run_host_tests.sh

      - name: Install ARM toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            cmake \
            ninja-build \
            gcc-arm-none-eabi \
            libnewlib-arm-none-eabi \
            libstdc++-arm-none-eabi-newlib

      - name: Build RP2350 firmware
        run: |
          cmake -S . -B build/rp2350-refresh-fix \
            -DCMAKE_BUILD_TYPE=Release \
            -DBP_PICO_PLATFORM=rp2350 \
            -DPICO_SDK_FETCH_FROM_GIT=TRUE \
            -DPICO_SDK_FETCH_FROM_GIT_TAG:STRING=2.3.0
          cmake --build build/rp2350-refresh-fix --parallel 2 --target all

      - name: Build RP2040 firmware
        run: |
          cmake -S . -B build/rp2040-refresh-fix \
            -DCMAKE_BUILD_TYPE=Release \
            -DBP_PICO_PLATFORM=rp2040 \
            -DPICO_SDK_FETCH_FROM_GIT=TRUE \
            -DPICO_SDK_FETCH_FROM_GIT_TAG:STRING=2.3.0
          cmake --build build/rp2040-refresh-fix --parallel 2 --target all

      - name: Upload verified firmware
        uses: actions/upload-artifact@v4
        with:
          name: deep-format-usb-refresh-fix-firmware
          path: |
            build/rp2350-refresh-fix/src/*.uf2
            build/rp2040-refresh-fix/src/*.uf2
          if-no-files-found: error

      - name: Commit verified source fix
        run: |
          rm -rf build/rp2350-refresh-fix build/rp2040-refresh-fix build-host
          git config user.name 'Marq-Dev'
          git config user.email '9102154+Marq-Dev@users.noreply.github.com'
          git add src/msc_disk.c
          git commit -m 'fix: preserve ejected USB medium during label refresh'
          git push origin HEAD:fix/deep-format-fatfs-work-area
